
export const RouterConfig = {
    counter: { path: '/' },
    fetchdata: { path: '/fetch' },
    factorial: { path: '/factorial' },
    forCallback: {path: '/forCallback' },
    task: { path: '/task' },
}
